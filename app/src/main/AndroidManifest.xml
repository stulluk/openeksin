<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".OpeneksinApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Openeksin"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Openeksin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".data.remote.CloudflareActivity"
            android:exported="false"
            android:label="@string/cloudflare_title"
            android:theme="@style/Theme.Openeksin" />

        <activity
            android:name=".data.remote.LoginActivity"
            android:exported="false"
            android:label="@string/login_title"
            android:theme="@style/Theme.Openeksin" />

        <activity
            android:name=".data.remote.WebPageActivity"
            android:exported="false"
            android:theme="@style/Theme.Openeksin" />
    </application>
</manifest>
